CMAKE_MINIMUM_REQUIRED (VERSION 2.6)
PROJECT (aja_fuzzy)

ADD_LIBRARY(aja_fuzzy SHARED fuzzifier.cpp) 
SET (EXTRA_LIBS ${EXTRA_LIBS} aja_fuzzy)
#INSTALL(TARGETS aja_fuzzy DESTINATION ${HelloCMake_SOURCE_DIR}/assets)
